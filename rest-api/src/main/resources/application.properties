# Spring boot application
spring.application.name = webframework-rest-api
spring.jpa.open-in-view=true
logging.level.org.hibernate.SQL=debug

server.port = 8080
management.server.port = 8081

### mysql
#spring.jpa.hibernate.ddl-auto=none
#spring.datasource.url=jdbc:mysql://10.10.101.160:3306/ab_oauth
#spring.datasource.username=root
#pring.datasource.password=anbang


### h2
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:~/webframework-rest
spring.datasource.username=root


user.service.version = 1.0.0

# Dubbo Config properties
## ApplicationConfig Bean
dubbo.application.id = webframework-rest-api
dubbo.application.name = webframework-rest-api

## Legacy QOS Config
dubbo.qos.port = 22223

## ProtocolConfig Bean
dubbo.protocol.id = dubbo
dubbo.protocol.name = dubbo
dubbo.protocol.port = 12345

# Dubbo Endpoint (default status is disable)
endpoints.dubbo.enabled = true

# Dubbo Health
## StatusChecker Name defaults (default : "memory", "load" )
management.health.dubbo.status.defaults = memory

# Enables Dubbo All Endpoints
management.endpoint.dubbo.enabled = true
management.endpoint.dubbo-shutdown.enabled = true
management.endpoint.dubbo-configs.enabled = true
management.endpoint.dubbo-services.enabled = true
management.endpoint.dubbo-references.enabled = true
management.endpoint.dubbo-properties.enabled = true

# Exposes all web endpoints
management.endpoints.web.exposure.include = *